# Aplicação
NODE_ENV=development
PORT=3000

# Banco de dados (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=sao_lourenco

# Banco usado pelos testes E2E (criado pelo init.sql do container)
DB_NAME_TEST=sao_lourenco_test
